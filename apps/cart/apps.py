from django.apps import AppConfig


class CartConfig(AppConfig):
    """
    Configuration class for the 'cart' Django app.

    The name attribute is set to 'apps.cart'.
    """
    name = 'apps.cart'
